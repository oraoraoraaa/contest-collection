# Cake Cutting

## Overall Logic

Represent the cake as a 2D integer array initialized to `0`, where `0` represents an uncut cell. Each time we read the starting point of the cut, directly simulating the cut through process instead of logging it in a buffer.
